<%# frozen_string_literal: true %>

<%# 
  This view exists only so Rails has a template to render for /anonymous-feedback.
  The actual UI is provided by the Ember route/template in the plugin frontend.
  Discourse's application layout mounts the Ember app; the template body can be empty.
%>
